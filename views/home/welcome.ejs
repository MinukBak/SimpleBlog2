<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="container mb-3">

      <div class="jumbotron">
        <h2>Welcome!</h2>
        <P>블로그를 방문해 주셔서 감사합니다!</p>
      </div>

    </div>

    <div class="container mb-3">
      <h3>Resent Posts</h3>

      <table class="board-table table table-sm border-bottom">

        <thead class="thead-light">
          <tr>
            <th scope="col">Title</th>
            <th scope="col" class="author">Author</th>
            <th scope="col" class="date">Date</th>
          </tr>
        </thead>

        <tbody>
          <% if(posts == null || posts.length == 0){ %>
            <tr>
              <td colspan=2> There is no data to show :( </td>
            </tr>
          <% } %>

          <% for (var l = posts.length, i = 0; i < 5; i++) { %>
            <% post = posts[i]%>
            <tr>
              <td>
                <a href="/posts/<%= post._id %>">
                  <div class="ellipsis"><%= post.title %></div>
                </a>
              </td>
              <td class="author">
                <a href="/posts/<%= post._id %>">
                  <div class="ellipsis"><%= post.author ? post.author.username : "" %></div>
                </a>
              </td>
              <td class="date">
                <span data-date="<%= post.createdAt %>"></span>
              </td>
            </tr>
          <% } %>
        </tbody>

      </table>

    </div>

  </body>
</html>
